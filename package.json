{
  "name": "obsidian-maki",
  "version": "0.1.2",
  "description": "Read and annotate PDF and EPUB documents, with markdown notes as the source of truth.",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.5.3",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc --noEmit && node esbuild.config.mjs production && node scripts/gen-third-party-licenses.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "licenses": "node scripts/gen-third-party-licenses.mjs",
    "licenses:check": "node scripts/gen-third-party-licenses.mjs --check",
    "bump": "node scripts/bump.mjs"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.0",
    "jsdom": "^29.1.1",
    "obsidian": "^1.7.2",
    "obsidian-typings": "^6.19.0",
    "pdfjs-dist": "^6.1.200",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "@zip.js/zip.js": "^2.8.26",
    "monkey-around": "^3.0.0"
  }
}
